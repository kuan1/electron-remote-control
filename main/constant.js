const resolve = require('./utils/resolve')

const scheme = 'electroncontrol'

const static = resolve('renderer')

const author = 'https://github.com/kuan1'

module.exports = {
  scheme,
  static,
  author,
}
